#include <iostream>

using namespace std;

int main() {

    class Student {
    public:
        int studentId;
        string name;
        int age;
        double score;
        char grade;
        bool status;
    };

    enum Color {Red, Blue, Yellow, Black, Pink};

    return 0;
}